function[f0,g0,G0] = Penalty(x)
    n = length(x); 
    gamma  = 10^-5;
    func = getPenalty(n,gamma); 
    f0 = eval(subs(func,x,x0)); 
    g = jacobian(func);
    g0 = (eval(subs(g,x,x0)))';
    G = hessian(func);
    G0 = eval(subs(G,x,x0));
   
end
